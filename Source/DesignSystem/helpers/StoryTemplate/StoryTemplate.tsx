// Copyright (c) Aigonix. All rights reserved.
// Licensed under the MIT license. See LICENSE file in the project root for full license information.

// Import React.
import React from 'react';

// Import Material UI components.
import { } from '@mui/material';

// Import related components.
import { } from '@dolittle/design-system';

// Export the related types. Also write the description as JsDoc of the component here.
/**
 * The props for a {@link StoryTemplate} component.
 */
export type StoryTemplateProps = {
    /**
     * The sx prop lets you add custom styles to the component, overriding the styles defined by Material-UI.
     */
    // sx?: SxProps;

    /**
     * The overrides prop gives you access to the underlying MuiButtonProps object, overriding the styles defined by the component and Material-UI.
     */
    // overrides?: Partial<ExtendButtonBase<ButtonTypeMap>>;
};

/**
 * Add a description of the component here.
 * @param {StoryTemplateProps} props - The {@link StoryTemplateProps}.
 * @returns A {@link StoryTemplate} component.
 */
export const StoryTemplate = ({ }: StoryTemplateProps) =>
    <></>;
